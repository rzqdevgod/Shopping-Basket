name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run tests
        run: docker-compose up --build
      - name: Run PHPStan
        run: docker-compose run app ./vendor/bin/phpstan analyse